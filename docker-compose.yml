version: '3.8'

services:
  mcp-server:
    build: .
    ports:
      - "8000:8000"
    environment:
      - EXCHANGE_RATE_API_KEY=${EXCHANGE_RATE_API_KEY}
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
    volumes:
      - ./mcp_server:/app/mcp_server
      - ./config:/app/config
